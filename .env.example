# Porta do servidor (opcional, padrão 3000)
PORT=3000

# Segredo do JWT (troque em produção)
JWT_SECRET=chave-secreta-dev

# (OPCIONAIS) Notificações de suporte por e-mail/Slack.
# Se não preencher, o chamado é criado normalmente e a notificação é ignorada.
EMAIL_USER=
EMAIL_PASS=
SLACK_WEBHOOK_URL=
